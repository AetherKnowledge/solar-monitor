{
  "private": true,
  "scripts": {
    "dev": "pnpm --dir web dev",
    "build:web": "pnpm --dir web build",
    "build": "pnpm build:web && pio run",
    "upload": "pio run -t upload",
    "uploadfs": "pio run -t uploadfs",
    "flash": "pnpm build:web && pnpm uploadfs && pnpm upload"
  }
}
